let a=15;
letb=70;
letc=a+b;
console.log(c);


letd=23;
let e=24;
let f=d+e;
console.log(f);

// whenever we observe a similar kind of codr repeated on our codebase.then we can define a function
// in which we will mention the codes.now whenever we are in need of that codes,we
// C